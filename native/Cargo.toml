[package]
name = "time"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
license = "MIT"
description = "coil-time native cdylib (chrono + Instant registry, C ABI coil_time_*)"
publish = false

[lib]
name = "time"
crate-type = ["cdylib", "rlib"]

[dependencies]
chrono = "0.4.45"
